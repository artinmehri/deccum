# Server-only environment variables.
# Copy this file to `.env.local` and fill in values locally.
# Never commit `.env.local` or real API keys.

# Optional. Used only by the server-side /api/chat route.
# Leave blank to use the built-in rules engine (recommended for public demos).
OPENAI_API_KEY=
